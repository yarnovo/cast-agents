# REQ-003 · cast-agents 拆 meta-hermes / demo-agents · 砍 cast-builtin-agents

- status: in-progress
- branch: feat/meta-hermes-demo-split
- assignee: cast-agents-maintainer (lead-claude 派 subagent)
- parent_scenario: 老板 5-9 拍 · meta agent 抽出独立 hermes 仓 · 7 demo 抽 demo-agents 可选包

## 描述

历史: `cast-builtin-agents` 仓装 8 个 yaml (1 meta + 7 服务 agent) · 平台启动一锅烩 sync 全部入库 (`ag_builtin_*`)。

问题:
1. **prod 不该装 demo** · 7 个服务 agent 是验证 / 演示用 · 不该自动入 prod cast-api
2. **meta 的特殊性丢了** · meta-xiaozao 跟 7 个服务 agent 同等地位 · 但 meta 持有 `meta.create_agent` 等特权工具 · 该跟工具实现一起走仓
3. **业务 agent 仓拖工具依赖** · cast-platform-tools 当前 `cast.create_agent` (meta-only) 跟 `cast.post` 等业务 agent 也用的 tool 混仓 · 责任不清

老板 5-9 拍方案: cast-builtin-agents archive · 拆成两个独立仓:
- `meta-hermes` · meta agent (阿空小造) 静态 hermes (yaml + 3 个 meta.* tool 实现) · prod 必装
- `demo-agents` · 7 个 demo agent yaml · prod 默认不装 · staging / dev opt-in (`CAST_INSTALL_DEMO_AGENTS=1`)

## acceptance

- [x] pyproject.toml 加 `meta-hermes` (主 dep) + `demo-agents` (`[demo]` extra · dev group 默认装)
- [x] 删 `src/cast_agents/builtin_sync.py` + `tests/test_builtin_sync.py`
- [x] `main.py` lifespan 改: import meta-hermes · sync_meta · env CAST_INSTALL_DEMO_AGENTS=1 时 import demo-agents · sync_demo_agents
- [x] tests/test_main_smoke.py 改: 验 meta tools 注册 · meta sync 调用 · demo sync env 控制 (3 case)
- [x] Dockerfile 改: ARG INSTALL_DEMO 控制 `--extra demo` · 删 `COPY cast-builtin-agents`
- [x] GHA 双 workflow 改: 删 clone cast-builtin-agents step · prod build INSTALL_DEMO=0 + env CAST_INSTALL_DEMO_AGENTS=0 · staging build INSTALL_DEMO=1 + env =1
- [x] README + CONTRACTS 上下游声明改 (meta-hermes 必 / demo-agents 可选 / cast-builtin-agents 已 archive)
- [x] 本地 uv run pytest 真过 13 cases (新增 3 lifespan case + 拿掉 builtin_sync 8 case)
- [ ] PR 提交 + lead review/merge → prod deploy 自动 → curl /api/agents 真 verify (只 meta · 不带 7 demo)
- [ ] 大仓 git rm submodule cast-builtin-agents · 注册 meta-hermes + demo-agents submodule

## RCA · 老 cast-builtin-agents 该 archive 的根因

- 单仓装 1 meta + 7 demo · 责任不清 (谁该 own?)
- prod 启动 lifespan 一锅 sync 8 yaml · production cast-api `agents` 表常年挂着 7 个 demo · 用户 / 真客户进来看到一堆 demo
- meta tool (`cast.create_agent`) 跟业务 tool (`cast.post`) 同仓 · meta 升级影响业务

## 不做

- 不动 cast-api 表结构 (id_override=ag_builtin_<slug> 兼容老数据)
- 不动 cast-app 前端 (旧 ag_builtin_* ID 不变)
- 不动 cast-platform-tools 的 `cast.create_agent` (向后兼容 · 等老板拍后续是否真删)

## 时间线

- 2026-05-09 · lead 派 subagent · meta-hermes + demo-agents 两仓建好 + 各自 pytest pass + push GitHub
- 2026-05-09 · cast-agents PR 提交
