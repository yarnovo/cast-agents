---
id: REQ-001
type: requirement
title: + POST /api/agent/run · 接 harness 新 run() 多轮 tool use loop API
status: in_progress
priority: P0
created: 2026-05-08
closed: null
assignee: cast-agents-maintainer
parent_gap: null
parent_scenario: null
related_repos:
  - akong/agent-harness  (上游 · 提供 run() / RunResult / Session / LLMClient)
  - cast/api  (上游 · chat_messages 表 + 3 endpoint)
  - cast/app  (下游 · 后续 /create 链路改调本 endpoint)
pr: null
related_analysis:
  - docs/architecture.md  (§2 6 件套 · §2.7 runtime)
related_guards:
  - tests/test_agent_run.py
---

# REQ-001 · + POST /api/agent/run · 接 harness 新 run() 多轮 tool use loop API

## 描述

上游 `akong-agent-harness@fc8c576` (#1 已 merge · 5-7 拍) 已落地 `runtime.run()` 新 API:
- 多轮 tool use loop · sync 一次性返 `RunResult` (老板 5-8 砍 streaming)
- 配套 `AgentDef` / `Session` (RdsSession 走 cast-api `/api/chat_messages`) / `LLMClient` (OpenAICompatibleClient)
- session_id 由 caller 自管 · 跨 FC invoke sticky 续上下文

cast-api `chat_messages` 表 + 3 endpoint (POST/GET/DELETE) 已 prod live · 链路 ready。

cast-agents 当前只有老 `/api/agent/tick` (sync 单轮 · 6 件套 fetch) · 缺新 `/api/agent/run` 入口接 `run()` API。本 REQ 加新 endpoint · 老 tick 保留 (cast-app /create 现链路依赖)。

## acceptance (lead 锁 · 5 条)

- [ ] `uv run pytest tests/ -v` → 全 pass (含新 `test_agent_run.py` 至少 4 case: 走通 / max_turns / LLMError → 502 / SessionUnavailable → 503)
- [ ] `grep -n "POST /api/agent/run\|/api/agent/run" src/cast_agents/main.py` → 至少 1 hit (含路由注册)
- [ ] `pyproject.toml` 锁 `akong-agent-harness` ≥ 0.1.0 / sha=fc8c576 · `uv lock` 同步 · uv.lock 含 fc8c576
- [ ] CONTRACTS.md 写本仓上下游 (cast-api + akong-agent-harness 上游 · cast-app 下游)
- [ ] PR merge 后 prod workflow 跑通 · `curl -X POST https://api.cast-agents.agentaily.com/api/agent/run -H 'Content-Type: application/json' -d '{...}'` → 200 + RunResult body

## 不做 (边界)

- ❌ 不动老 `/api/agent/tick` (cast-app 现链路依赖)
- ❌ 不实现 streaming (老板 5-8 砍)
- ❌ 不改 cast-api chat_messages schema (上游已 live)
- ❌ 不实现 cast-app 端切换 (下游另起)
- ❌ 不引入 sandbox / FC 运行时改造 (维持现 FC v3 Custom Container)

## 4 问 (maintainer 完工后答)

### 1. IMPL (实现路径)

(完工填)

### 2. DONE (1:1 对账 acceptance)

(完工填)

### 3. SELF-VERIFY (自验跑通)

(完工填)

### 4. GUARDS (防 regression)

(完工填)

## review (lead 完工时填)

- acceptance #1: 待跑
- acceptance #2: 待跑
- acceptance #3: 待跑
- acceptance #4: 待跑
- acceptance #5: 待跑
- 决策: -
- 验收时间: -

## 时间线

- 2026-05-08 lead 派活 (commit pending)
- 2026-05-08 maintainer 接活 (status: open → in_progress)

## 完工标准

- ✅ acceptance 每条 ✓ 真跑
- ✅ 4 问真证据 (commit hash + PR link · 不空口白牙)
- ✅ related_analysis ≥ 1 wiki path (docs/architecture.md)
- ✅ related_guards ≥ 1 test/CI path (tests/test_agent_run.py)
