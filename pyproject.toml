[project]
name = "cast-agents"
version = "0.1.0"
description = "Cast 平台 agent 后端装配商 · 装 akong-agent-harness + cast-platform-tools · FC v3 部署"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "akong-agent-harness @ git+https://github.com/yarnovo/akong-agent-harness.git@req-meta-package-8repo-split",
    "cast-platform-tools @ git+https://github.com/yarnovo/cast-platform-tools.git@req-bump-akong-meta-8repo-split",
    "fastapi==0.136.1",
    "uvicorn[standard]==0.46.0",
    "pydantic==2.13.4",
    "pydantic-settings==2.6.1",
    "pyyaml>=6.0",
    "httpx>=0.27",
]

[dependency-groups]
dev = [
    "pytest==9.0.3",
    "pytest-mock==3.15.1",
    "pytest-asyncio>=1.0.0,<2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cast_agents"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
